server side:

git hooks:
post-receive

    GIT_WORK_TREE=/home/zgemba/webapps/pksk_test/myproject git checkout -f master
    GIT_WORK_TREE=/home/zgemba/webapps/django/myproject git reset --hard

    fetch drevo
    reset head
    aktiviraj venv
    if git diff --name-status HEAD HEAD~1 | grep -q 'app/migrations/versions'; then
        manage.py db upgrade
        echo "Database upgrade"
    fi

    if git diff --name-status HEAD HEAD~1 | grep -q 'requirements.txt'; then
        pip3 install -r -U requirements.txt
        echo "Requirements changed"
    fi

    apache restart


